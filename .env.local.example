# App Configuration (Amplify-safe names, no AWS_ prefix)
APP_REGION=ap-northeast-2

# AWS IoT Core Endpoint
# Find this in AWS IoT Core Console > Settings > Device data endpoint
IOT_ENDPOINT=xxxxxxxxxxxxx-ats.iot.ap-northeast-2.amazonaws.com

# DynamoDB Tables (optional, defaults are set in code)
# DYNAMODB_DISPENSERS_TABLE=dispensers
# DYNAMODB_DISPENSE_LOGS_TABLE=dispense_logs
